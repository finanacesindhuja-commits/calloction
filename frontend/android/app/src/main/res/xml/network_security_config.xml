<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTPS calls to Render backend in both debug and release -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">calloction.onrender.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Base config: allow all HTTPS, block HTTP (secure by default) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug only: allow all traffic for local testing -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
